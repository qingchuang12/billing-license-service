# 文档总索引（docs/）

> billing-license-service 的文档导航。按角色/用途检索，避免「文档散落、找不到」。

## 产品与架构文档（对外/设计）

| 文档 | 适用场景 |
|---|---|
| [`README.md`](./README.md) | 项目入口：定位、技术栈、快速开始、**实现现状**（与代码同步维护） |
| [`接口调用时序图.md`](./接口调用时序图.md) | **实现现状**：业务域梳理 + **28 个端点**的端到端调用时序图（Mermaid）、横切机制（**鉴权三档**/traceId/响应壳/审计）、外部依赖与类→方法速查 |
| [`架构与业务流程设计.md`](./架构与业务流程设计.md) | **设计建议稿 · 非实现现状**：由「架构设计」与「业务流程」合并而成的候选方案与路线图参考；文首列合并全集偏离清单 |
| [`授权设计方案.md`](./授权设计方案.md) | **设计建议稿 · 非实现现状**：由上述两篇拆出的「授权 / License 设计」专项（算法、payload、格式、机器码、离线验证、换机、密钥、防破解）；文首列 License 相关偏离清单 |

> 标注「设计建议稿」的文档保留原始建议（含未采纳选项），**不可作为接口/字段契约依据**；契约以 `README.md` 的 API 段与代码为准。

## 运维与数据库

| 文档 / 目录 | 适用场景 |
|---|---|
| [`../scripts/README.md`](../scripts/README.md) | 运维脚本、构建/部署、Flyway 迁移清单与环境变量速查（**日常操作入口**） |
| [`上线准备工作.md`](./上线准备工作.md) | **上线执行手册**：Go/No-Go 阻断项清单、签名密钥生成（已实测命令）与轮换、支付渠道设置与回调注册、环境变量基线、DB 迁移与备份、安全加固、部署与验收清单。上线前逐节执行。 |
| `src/main/resources/db/migration/` | Flyway 迁移脚本（**现存 V1–V4**，2026-09-22 回目录核实；**下一个可用版本号为 V5**）。注意：本文档曾写「V1–V11」属过期描述，V10/V11 并不存在，编号请以目录实际文件为准。变更请遵循 `scripts/README.md` 的「禁止合并/禁止首次部署后修改」规则 |

## 内部工作文档（非交付物）

| 文档 | 说明 |
|---|---|
| [`plan-4.0.md`](./plan-4.0.md) | **并行 plan A · 退款**：用户端**直接退款** + **按使用时间折算金额**（2026-09-22 拍板）。含归属/资格校验、复用 `AdminService.refundOrder`、退款窗口、限流与审计。折算公式与各渠道「部分退款」能力确认后动手。 |
| [`plan-5.0.md`](./plan-5.0.md) | **并行 plan B · 凭证体系治理**：许可证激活与兑换码激活的重叠治理（单一凭证激活端点 + 统一绑定内核 + 归属校验），跨仓库 `ai-tools`。 |
| [`plan-6.0.md`](./plan-6.0.md) | **并行 plan C · 统一登录**：管理员与消费者统一账号密码登录（`users.role`、`JwtAuthFilter` 按角色授权、管理控制台改用密码登录）。 |
| 已完成 plan | **不保留归档**（按「完成即清理」纪律）：历史 v1.0–v3.5 各主题 plan 已随其完成删除，需要历史结论请查 `git log -- docs/`。 |

> **分工**：A 走退款链路；B 改激活/凭证链路；C 改鉴权模型。三者只在「都以 users 表与客户身份为支点」处交汇——
> C 的 `users.role` 落地后，B 的「归属校验」可直接复用同一套用户身份口径，故并发时建议**先 C 后 B**。

## 如何新增文档

- 产品/架构类文档放在 `docs/`（`README.md` / `架构与业务流程设计.md` / `授权设计方案.md`）；
- 运维/脚本类放在 `scripts/`（见其 `README.md`）；
- 当前任务与整改统一写入**活动 plan** `docs/plan-<x.y>.md`（文件名带版本号，新建时版本号递增）；原则上只保留**一个**活动 plan，若确需并行（如新功能线 + 上线验证线），必须在本索引中登记两者分工与状态；完成的 plan **直接删除**（不建 `docs/archive/`、不留备份），历史结论从 `git log -- docs/` 追溯；
- 设计建议类文档必须在文首声明「设计建议 · 非实现现状」并给出与实现的偏离清单，避免与实现文档混淆。
